// import { useLayoutForceAtlas2 } from "@react-sigma/layout-forceatlas2";
// import { useLayoutCircular } from "@react-sigma/layout-circular";
// import { useLayoutRandom } from "@react-sigma/layout-random";
// import { useSigma } from "@react-sigma/core";
// import { animateNodes } from "sigma/utils";

export const LayoutControls = () => {
    /*
    const sigma = useSigma();
    const { positions: circularPositions } = useLayoutCircular();
    const { positions: randomPositions } = useLayoutRandom();
    const { assign: assignFA2 } = useLayoutForceAtlas2();

    const handleCircular = () => {
        animateNodes(sigma.getGraph(), circularPositions(), { duration: 1000 });
    };

    const handleRandom = () => {
        animateNodes(sigma.getGraph(), randomPositions(), { duration: 1000 });
    };

    const handleForceAtlas2 = () => {
        assignFA2();
    };

    return (
        <div className="flex flex-col gap-2 p-3 bg-white shadow-xl min-w-[140px] rounded-lg border border-gray-200">
            <span className="text-[10px] font-bold text-gray-500 uppercase px-1 tracking-widest mb-1">Layouts</span>
            <button
                onClick={handleForceAtlas2}
                className="px-3 py-2 text-sm bg-gray-50 text-gray-700 rounded hover:bg-[#0d99ff]/20 hover:text-[#0d99ff] transition-all text-left flex items-center gap-2 group border border-transparent hover:border-[#0d99ff]/30"
            >
                <div className="w-2 h-2 rounded-full bg-[#0d99ff] group-hover:scale-125 transition-transform"></div>
                Force Atlas 2
            </button>
            <button
                onClick={handleCircular}
                className="px-3 py-2 text-sm bg-gray-50 text-gray-700 rounded hover:bg-emerald-500/20 hover:text-emerald-500 transition-all text-left flex items-center gap-2 group border border-transparent hover:border-emerald-500/30"
            >
                <div className="w-2 h-2 rounded-full bg-emerald-500 group-hover:scale-125 transition-transform"></div>
                Circular
            </button>
            <button
                onClick={handleRandom}
                className="px-3 py-2 text-sm bg-gray-50 text-gray-700 rounded hover:bg-purple-500/20 hover:text-purple-500 transition-all text-left flex items-center gap-2 group border border-transparent hover:border-purple-500/30"
            >
                <div className="w-2 h-2 rounded-full bg-purple-500 group-hover:scale-125 transition-transform"></div>
                Random
            </button>
        </div>
    );
    */
    return null;
};
